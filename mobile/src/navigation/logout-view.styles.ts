import { StyleSheet } from 'react-native';

//----------------------------------------------------------------------------
export const LogoutStyle = StyleSheet.create({
    container: {
        alignItems: 'flex-end',
        paddingRight: 10
    },
    image: {
        height: 25,
        width: 25,
        tintColor: '#fff'
    }
});
